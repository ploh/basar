#error_explanation{ class: messages.empty? && "hidden" }
  %h2= "#{pluralize(messages.count, "error")} prohibited this transaction from being valid:"
  %ul
    - messages.each do |msg|
      %li= msg
