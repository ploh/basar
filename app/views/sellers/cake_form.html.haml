%h2 Kuchen backen für Vorkaufsrecht

= form_for(@user, url: sellers_cake_path, html: {method: :put}) do |f|
  - if @user.errors.any?
    #error_explanation
      %h2= "Fehler haben das Speichern verhindert:"
      %ul
        - @user.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :cake
    = f.check_box :cake

  .actions
    = f.submit 'Speichern'
